@model IEnumerable<Sport>

@{
    ViewData["Title"] = "Sports";
}

<h2>Listes des sports</h2>

<div>
    <a asp-controller="Sport" asp-action="Create">Ajouter un sport</a> |
</div>

<table class="table">

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
            </tr>
        }
    </tbody>
</table>


